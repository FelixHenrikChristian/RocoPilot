<Page
    x:Class="RocoPilot.Views.Windows.AutoBattleConfigPages.AutoBattleLegendaryConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer
        HorizontalContentAlignment="Stretch"
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Auto">
        <StackPanel Style="{StaticResource ConfigPageRootStyle}">
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource ConfigPageTitleStyle}" Text="传说精灵挑战" />
                <TextBlock
                    Style="{StaticResource ConfigPageDescriptionStyle}"
                    Text="为传说精灵挑战预留的独立战斗配置。" />
            </StackPanel>

            <Border Style="{StaticResource SectionCardStyle}">
                <StackPanel>
                    <Grid Style="{StaticResource SectionCardHeaderStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource SymbolThemeFontFamily}"
                            FontSize="18"
                            Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                            Glyph="&#xE734;" />
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="功能开发中" />
                            <TextBlock
                                Style="{StaticResource SectionDescriptionStyle}"
                                Text="当前版本尚未实现识别与执行逻辑，暂不提供可保存的专属选项。" />
                        </StackPanel>
                    </Grid>

                    <Grid MinHeight="320" Padding="24">
                        <StackPanel
                            MaxWidth="440"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12">
                            <Border
                                Width="64"
                                Height="64"
                                HorizontalAlignment="Center"
                                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                CornerRadius="16">
                                <FontIcon
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="28"
                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                    Glyph="&#xE734;" />
                            </Border>
                            <TextBlock
                                HorizontalAlignment="Center"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="传说精灵挑战暂未实现" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                TextAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="页面结构已预留。后续接入运行逻辑时，可增加专属释放规则，并复用现有公共执行序列。"
                                TextWrapping="Wrap" />
                            <Border
                                Margin="0,4,0,0"
                                Padding="12,8"
                                HorizontalAlignment="Center"
                                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                CornerRadius="8">
                                <TextBlock
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind Editor.SharedPresetSummary, Mode=OneWay}" />
                            </Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
